---
import { storyblokEditable } from '@storyblok/astro';

const { blok, icon } = Astro.props;
---

<div {...storyblokEditable(blok)} class="con-item">
  {icon?.filename && (
    <img
      class="con-item-icon"
      src={icon.filename}
      alt={icon.alt || ''}
      width="24"
      height="24"
    />
  )}
  {blok.con && <p>{blok.con}</p>}
</div>

<style>
  .con-item {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
  }

  .con-item-icon {
    flex: 0 0 auto;
    display: block;
  }

  .con-item p {
    margin: 0;
  }
</style>
