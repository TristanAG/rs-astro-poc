---
import { storyblokEditable } from '@storyblok/astro';
import StoryblokComponent from '@storyblok/astro/StoryblokComponent.astro';

const { blok, class: className = 'latest-article-links' } = Astro.props;
---

<ul {...storyblokEditable(blok)} class={className}>
  {(blok.latest_article_links ?? blok.latest_article_link ?? [])?.map((item) => (
    <li>
      <StoryblokComponent blok={item} />
    </li>
  ))}
</ul>

<style>
  .latest-article-links {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
</style>
